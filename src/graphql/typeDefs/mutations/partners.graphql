type PartnersMutation {
  create(partner: PartnerCreateInput!): Partner! @auth(requires: "partners")
  delete(id: ID!): ID @auth(requires: "partners")
  partner(id: ID!): PartnerMutation @auth(requires: "partners")
}
