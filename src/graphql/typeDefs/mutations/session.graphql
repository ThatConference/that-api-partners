type PartnerSessionMutation {
  add(session: PartnerSessionAddInput!): Session! @auth(requires: "admin")
  remove: ID @auth(requires: "admin")
  update(session: PartnerSessionUpdateInput!): Session! @auth(requires: "admin")
}
